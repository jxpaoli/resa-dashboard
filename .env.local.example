# Copie ce fichier en .env.local (local) et mets les mêmes dans Cloudflare Pages.
# Tant que les 3 sont vides → l'app tourne en mode mock (localStorage).
# Dès qu'elles sont remplies → l'app utilise Supabase (auth + données + temps réel).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_ID=
